name: MIGRAR FEEDBACK ANTIGO PARA A BASE GLOBAL

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Migração antiga desativada. O feedback atual usa diretamente a base central.'
