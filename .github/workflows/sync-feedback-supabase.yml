name: Verificar feedback centralizado

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-feedback:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verificar integração Supabase
        shell: bash
        run: |
          grep -qF 'https://uxwbbaeupemihonwyszu.supabase.co/functions/v1/feedbacks' index.html
          grep -qF 'id="yunamyst-feedback-central-js"' index.html
          ! grep -qF 'yunamyst_feedbacks_v2' index.html
          echo 'OK: feedbacks usam uma base online central e não localStorage.'
