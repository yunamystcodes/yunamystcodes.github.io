name: FEEDBACK GLOBAL — FORÇAR CORREÇÃO FINAL

on:
  workflow_dispatch:

permissions:
  contents: write

# Este workflow foi desativado no push porque versões anteriores entravam
# em corrida com os restantes workflows e podiam quebrar o index.html.
# A sincronização global é agora mantida pelo feedback no próprio index.

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow antigo de feedback global desativado para evitar alterações concorrentes no index.'
